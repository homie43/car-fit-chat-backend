# CarFit Chat - Дорожная карта

## ✅ Фаза 1: Классический RAG (Выполнено - 28.02.2026)

### Реализованные возможности:
- **Структурированный поиск**: Поиск по марке, модели, году, типу КПП
- **RAG контекст**: Результаты поиска добавляются в промпт LLM ПЕРЕД генерацией ответа
- **Честный фолбэк**: Сообщение "нет данных в базе" когда поиск не находит результатов
- **Интеграция описаний**: 108 автомобилей с AI-генерированными описаниями
- **Умная фильтрация**: Используются только автомобили С описаниями для рекомендаций

### Как это работает:
```
Запрос пользователя → Извлечение предпочтений → Поиск по БД →
→ Добавление результатов в контекст → Ответ LLM
```

### Ключевые улучшения:
- ✅ LLM теперь использует РЕАЛЬНЫЕ данные из базы
- ✅ Нет галлюцинаций и выдуманных рекомендаций
- ✅ Честная коммуникация когда данных нет
- ✅ Описания включают интересные факты и детали

---

## 🔜 Фаза 2: Векторный RAG (Будущее улучшение)

### Зачем нужен векторный поиск?

**Текущие ограничения:**
- Структурированный поиск требует точных совпадений по полям (марка, модель и т.д.)
- Пользователь должен указывать технические параметры
- Не понимает естественноязыковые запросы типа "семейный автомобиль для города"

**Преимущества векторного поиска:**
- 🎯 **Семантическое понимание**: Находит машины по СМЫСЛУ, а не только по ключевым словам
- 🗣️ **Естественный язык**: "надежный седан" → Toyota Camry, Honda Accord
- 🔍 **Лучшие совпадения**: Похожие авто даже без точных характеристик
- 📈 **Выше качество**: Понимает контекст и намерение пользователя

### План реализации

#### 1. Включить pgvector в Neon Database

```sql
-- Включить расширение pgvector (уже доступно в Neon.tech)
CREATE EXTENSION IF NOT EXISTS vector;

-- Добавить векторную колонку в таблицу car_variants
ALTER TABLE car_variants
ADD COLUMN description_embedding vector(768);

-- Создать индекс для быстрого поиска похожих векторов
CREATE INDEX car_variants_embedding_idx
ON car_variants
USING ivfflat (description_embedding vector_cosine_ops)
WITH (lists = 100);
```

#### 2. Выбрать модель для эмбеддингов

##### Вариант A: Sentence Transformers (Рекомендуется для MVP)
**Модель**: `paraphrase-multilingual-mpnet-base-v2`

**Плюсы:**
- ✅ Бесплатная и open-source
- ✅ Работает локально (нет затрат на API)
- ✅ Отличная поддержка русского языка
- ✅ Размерность: 768 (оптимально для производительности)
- ✅ Легко интегрируется с Node.js

**Установка:**
```bash
npm install @xenova/transformers
```

**Использование:**
```typescript
import { pipeline } from '@xenova/transformers';

const embedder = await pipeline(
  'feature-extraction',
  'sentence-transformers/paraphrase-multilingual-mpnet-base-v2'
);

const embedding = await embedder(text, { pooling: 'mean', normalize: true });
```

##### Вариант B: Яндекс Embeddings API
**Плюсы:**
- ✅ Нативная поддержка русского языка
- ✅ Облачное решение (не требует локальных ресурсов)

**Минусы:**
- ⚠️ Требует API ключ
- ⚠️ Стоимость: ~$1-5/месяц для малых проектов

##### Вариант C: Jina AI Embeddings
**Плюсы:**
- ✅ Есть бесплатный tier
- ✅ Мультиязычная поддержка
- ✅ Быстрый API

**Минусы:**
- ⚠️ Внешняя зависимость

#### 3. Скрипт генерации эмбеддингов

**Файл**: `src/scripts/generate-embeddings.ts`

```typescript
/**
 * Генерация эмбеддингов для всех описаний автомобилей
 * Запускать один раз после добавления/обновления описаний
 */
import { prisma } from '../shared/utils/prisma';
import { pipeline } from '@xenova/transformers';

async function generateEmbeddings() {
  const embedder = await pipeline(
    'feature-extraction',
    'sentence-transformers/paraphrase-multilingual-mpnet-base-v2'
  );

  const cars = await prisma.carVariant.findMany({
    where: { description: { not: null } },
    select: { id: true, description: true, name: true },
  });

  console.log(`Генерация эмбеддингов для ${cars.length} автомобилей...`);

  for (const car of cars) {
    // Создаём богатый текст для эмбеддинга
    const text = `${car.name}. ${car.description}`;

    // Генерируем эмбеддинг
    const result = await embedder(text, { pooling: 'mean', normalize: true });
    const embedding = Array.from(result.data);

    // Сохраняем в БД
    await prisma.$executeRaw`
      UPDATE car_variants
      SET description_embedding = ${embedding}::vector
      WHERE id = ${car.id}
    `;
  }

  console.log('✅ Эмбеддинги сгенерированы!');
}
```

#### 4. Гибридный поиск (Структурированный + Векторный)

**Объединяем оба подхода для лучших результатов:**

```typescript
async searchCarsHybrid(params: {
  query: string;           // Естественноязыковой запрос
  marka?: string;          // Опциональные фильтры
  yearFrom?: number;
  limit?: number;
}) {
  // 1. Векторный поиск (семантическое сходство)
  const queryEmbedding = await generateEmbedding(params.query);

  const vectorResults = await prisma.$queryRaw`
    SELECT *,
           1 - (description_embedding <=> ${queryEmbedding}::vector) as similarity
    FROM car_variants
    WHERE description IS NOT NULL
    ${params.marka ? `AND model.brand.name ILIKE '%${params.marka}%'` : ''}
    ORDER BY description_embedding <=> ${queryEmbedding}::vector
    LIMIT 20
  `;

  // 2. Структурированный поиск (точные совпадения)
  const structuredResults = await carsService.searchCarsForRAG({
    marka: params.marka,
    yearFrom: params.yearFrom,
    limit: 20,
  });

  // 3. Ре-ранжирование и объединение результатов
  const combined = rerank(vectorResults, structuredResults);

  return combined.slice(0, params.limit || 5);
}
```

#### 5. Соображения по производительности

**Оптимизация индексов:**
- `ivfflat` индекс: Быстрый приблизительный поиск
- Параметр `lists`: 100 для ~10K векторов, 1000 для ~1M векторов
- Пере-индексация после массового обновления эмбеддингов

**Производительность запросов:**
- Векторный поиск: ~10-50мс для 100K векторов
- Гибридный поиск: ~20-100мс всего
- Кэширование эмбеддингов для частых запросов

### Оценка трудозатрат

| Задача | Время | Сложность |
|--------|-------|-----------|
| Включить pgvector | 10 мин | Легко |
| Выбрать и настроить модель эмбеддингов | 1-2 часа | Средне |
| Скрипт генерации эмбеддингов | 2-3 часа | Средне |
| Реализация гибридного поиска | 3-4 часа | Средне |
| Тестирование и оптимизация | 2-3 часа | Средне |
| **Итого** | **1-2 дня** | **Средне** |

### Метрики успеха

**До (Классический RAG):**
- Запрос: "Toyota Camry AT" → Находит точные совпадения
- Запрос: "семейный седан" → Может ничего не найти (нет точного совпадения)

**После (Векторный RAG):**
- Запрос: "Toyota Camry AT" → Находит точные совпадения (как и раньше)
- Запрос: "семейный седан" → Находит Camry, Accord, Passat (семантическое совпадение!)
- Запрос: "надежный автомобиль для города" → Множество релевантных результатов

---

## 📊 Фаза 3: Продвинутые возможности (Долгосрочно)

### 3.1 Мультимодальный поиск
- Поиск по изображению: Загрузка фото авто → Поиск похожих машин
- Голосовые запросы: Speech-to-text → Векторный поиск

### 3.2 Персонализированные рекомендации
- Анализ истории пользователя
- Коллаборативная фильтрация
- Обучение предпочтениям из взаимодействий

### 3.3 Интеграция цен в реальном времени
- Интеграция с Auto.ru API
- Предсказание цен
- Анализ рыночных трендов

### 3.4 Продвинутая аналитика
- Отслеживание поведения пользователей
- A/B тестирование
- Метрики качества поиска

---

## 🎯 Текущий статус (28.02.2026)

**Выполнено:**
- ✅ Фаза 1: Классический RAG со 108 описаниями автомобилей
- ✅ Структурированный поиск по марке, модели, году, КПП
- ✅ Логика "нет данных в базе" для честных ответов

**В процессе:**
- 🔄 Нет

**Следующие шаги:**
- 📋 Сгенерировать больше описаний (расширить со 108 до 500-1000)
- 📋 Собрать отзывы пользователей о качестве текущего RAG
- 📋 Спланировать реализацию Фазы 2 (Векторный поиск)

---

## 📝 Примечания

- Фаза 2 (Векторный поиск) - это **опциональное** улучшение
- Текущий Классический RAG хорошо работает для структурированных запросов
- Векторный поиск рекомендуется когда:
  - Запросы пользователей становятся более естественноязыковыми
  - Нужно семантическое понимание
  - База данных растёт beyond 1000 автомобилей с описаниями
